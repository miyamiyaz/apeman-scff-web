/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

"use strict";

var extend = require('extend'),
    scff = require('./scff');

var lib = extend({}, scff);
lib.scff = scff;

module.exports = lib;