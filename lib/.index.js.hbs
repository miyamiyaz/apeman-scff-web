/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

"use strict";

var extend = require('extend'),
    scaffold = require('./scaffold');

var lib = extend({}, scaffold);
lib.scaffold = scaffold;

module.exports = lib;